# 更新记录

## 0.9.1 Beta — 2026-09-12

- 翻译进行时在 Edge 扩展图标右下角显示绿色三点徽标，完成、停止、报错或切换页面后自动清除；弹窗内不再重复显示三点动画。
- 首页介绍改为“为方便使用本地模型阅读网页而做”，语气更友好。
- 尝试放大图标主体，但生成候选图缺少真实透明通道，因此未替换现有图标，避免降低浏览器显示质量。

## 0.9.0 Beta — 2026-09-12

- 推荐模型调整为 Qwen2.5 3B「极致速度」、Qwen3.5 4B「日常推荐」和 Qwen3.5 9B「质量悠闲」，不再推荐 1.7B。
- 设置页允许填写任意已安装的本机 Ollama 模型；首次连接会列出其他本机模型。
- 新增 OpenAI-compatible 外部 API 接入。API Key 只保存在浏览器本地、不会导出或写入后台日志，远程地址必须使用 HTTPS。
- 修复中日韩固定术语边界可能出现的多余空格，并增强模型评测与打包校验。
- 修复无 BOM 的 UTF-8 启动脚本在 Windows PowerShell 5.1 中可能被错误解析为缺少大括号的问题。
- 安装器现在会自动发现 Python 3.10+；虚拟环境损坏、移动目录或原 Python 被卸载后，可自动重建且保留 `host/config.json`。
- 未安装 Python 时显示可直接运行的安装命令；受管电脑无法写入登录自启动项时，核心安装仍可完成，并提示使用 `start-host.cmd`。
- 新增 `diagnose.cmd`，自动检查 Windows、PowerShell、Python 环境、优诺后台、Ollama API 与模型，并生成不含 token/API Key 的诊断报告。
- 首次连接不再强制要求已安装 Ollama 模型；只使用外部 API 的用户可以先完成本机服务连接，再进入设置配置提供商。
- 发布包检查新增关键脚本非空、长度及 UTF-8 解码验证，避免空白或截断的安装脚本进入 ZIP。
- 补充全新 Windows 电脑的 Python/Ollama 安装命令、完整解压提醒、自动修复说明和无自启动权限时的替代步骤。

## 0.8.0 — 2026-09-11

- 按完整网站来源保存自动翻译偏好，修复跨网站继承与同站多标签页停止问题。
- 将原文、已渲染文本和任务代号分离，切换语言、停止及 SPA 更新后丢弃旧响应。
- 增加等待队列去重、有界缓存、断开节点清理与内部滚动容器监听。
- 修复可编辑区域、嵌套代码排除和划词范围；完整译文支持润色，结果支持复制和 Esc 关闭。
- 重做纸白/墨绿阅读界面；新增已开启网站管理、模型检查及作者入口。
- 首次设置只能选择已安装模型；无模型时不能完成。无效设置导入不会覆盖现有配置。
- 支持 Qwen3.5 9B，保留 Qwen2.5 3B 与试验性的 Qwen3 1.7B。调整结构化翻译提示词，保护术语与 URL，检测截断和格式错误。
- 限制后台来源、请求格式、大小、模型与并发；使用强随机令牌和 loopback 配置，忽略系统代理。
- 安装检查退出码和服务健康，后台使用绝对脚本路径；打包读取 manifest 版本并补齐说明、测试与 MIT 许可证。
- 增加 Python、后台状态和浏览器 DOM 回归测试以及 CI 配置。

升级后请重新加载扩展、刷新网页，再为需要的网站开启翻译。0.8.0 仍是面向实际使用反馈的 Beta，不宣称全部站点兼容或译文完全准确。

### 启动修复

- 后台直接使用 pythonw 启动，不再依赖 Windows Script Host/VBS。
- 缺少初始化文件时显示简明提示；支持自定义本机服务端口。
- 补充 pythonw 日志和连接地址验证；远程地址不会收到访问令牌。
